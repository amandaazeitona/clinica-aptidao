<%= form_with(model: @service, local: true) do |form| %>

  <table class="table">
    <tr>
      <th> Nome </th>
      <th> Preço </th>
    </tr>

    <tr>
      <td><%= form.text_field :name, class: "form-control" %></td>
      <td><%= form.text_field :price, class: "form-control" %></td>
    </tr>
  </table>


  <div class="actions">
    <%= form.hidden_field :macro_service_id, value: params[:format] %>
    <%= submit_tag "Salvar", class: "btn btn-success"%>
  </div>

<% end %>
